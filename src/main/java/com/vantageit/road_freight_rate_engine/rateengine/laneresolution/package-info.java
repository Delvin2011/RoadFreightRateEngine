/**
 * Pipeline Stage 2: lane resolution & distance computation. Resolves an already-validated
 * request's origin/destination locations to zones, builds the lane key, and resolves the
 * distance (operator override or the pre-computed distance matrix).
 */
package com.vantageit.road_freight_rate_engine.rateengine.laneresolution;
